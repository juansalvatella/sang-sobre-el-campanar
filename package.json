{
  "name": "sang-sobre-el-campanar",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^4.16.19"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
